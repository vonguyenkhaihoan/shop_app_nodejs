const url = 'http://192.168.88.101:3000';

const signup = "$url/api/signup";
const signin = "$url/api/signin";
const tokenValid = "$url/api/tokenIsValid";

//user
const user = "$url/";

//api
const apiProducts = "$url/api/products";

const apirateProducts = "$url/api/rate-product";

const apidealofdayProducts = "$url/api/deal-of-day";

const apiTopRateProducts = "$url/api/top-rate";

const apiAddToCart = "$url/api/add-to-cart";

const apiRemoveAToCart = "$url/api/remove-a-to-cart";

const apiRemoveAllCart = "$url/api/empty-cart";

const apiRemoveProductToCart = "$url/api/remove-from-cart";

const apiSaveUserAddress = "$url/api/save-user-address";

const apiUpdateUser = "$url/api/update-user";

const apiAllCaterogy = "$url/api/categories";

//orderproduct
const apiOrderProduct = "$url/api/order";

const apiMyOrder = "$url/api/orders/me";

//admin
const addProduct = "$url/admin/add-product";

const getAllProduct = "$url/admin/get-products";

const deleteProducts = "$url/admin/delete-product";

const getAllOrder = "$url/admin/get-orders";

const changeStatusOrder = "$url/admin/change-order-status";

const getAnalytics = "$url/admin/analytics";

const adminUpdate = "$url/admin/update-admin";

const totalOrders = "$url/api/total-order";

const totalMoneyOrder = "$url/admin/api/total-revenue";

const totalProducts = "$url/admin/total-product";

const apiaddCategory = "$url/create-categories";

const apideleteCategory = "$url/delete-categories";

const apiUpdateCategory = "$url/api/update-categories";

const apiCatePercent = "$url/api/categoriesPercentage";



